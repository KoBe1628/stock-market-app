# == General ignore rules ==
# Ignore common operating system generated files
.DS_Store             # macOS specific files
Thumbs.db             # Windows specific files
.gitkeep              # Empty files for Git to track empty folders (if used)

# Ignore temporary files, logs, and sensitive configurations
*.log                 # Log files
*.temp                # Temporary files
*.tmp
.env                  # Environment variables file (NEVER COMMIT SENSITIVE INFO)
.env.local            # Local environment variables

# == IDE specific ignores ==
# Ignore files generated by popular IDEs
.idea/                # IntelliJ / Android Studio project files
.vscode/              # VS Code workspace settings
*.iml                 # IntelliJ module files
.cproject             # Eclipse project files
.project              # Eclipse project files
.settings/            # Eclipse settings

# == Flutter / Dart specific ignores (for projects inside 'mobile' folder) ==
# Flutter build artifacts and dependencies
/mobile/.dart_tool/
/mobile/.flutter-plugins
/mobile/.flutter-plugins-dependencies
/mobile/.packages
/mobile/.pub-cache/
/mobile/build/                 # Main Flutter build output directory
/mobile/ios/.symlinks/         # iOS specific build links
/mobile/android/.gradle/       # Android Gradle cache
/mobile/android/app/build/     # Android app build output
/mobile/android/local.properties # Android SDK location (user-specific)
/mobile/web/flutter_bootstrap.js # Specific to Flutter web build output, if you build Flutter for web from the mobile project

# == Web (HTML/CSS/JS) specific ignores (for projects inside 'web' folder) ==
# Node.js and JavaScript specific ignores
/web/node_modules/      # Node.js dependency folder (very common if using npm/yarn for web assets)
/web/dist/              # Common web build output folder (e.g., for Webpack, Parcel, Rollup, or even your custom build)
/web/build/             # Another common web build output folder for web projects
/web/out/               # Another common web build output folder (e.g., Next.js)
/web/coverage/          # Test coverage reports for web
/web/npm-debug.log* # npm debug logs
/web/yarn-error.log* # Yarn error logs
/web/.parcel-cache/     # Example for Parcel bundler cache
/web/.vite/             # Example for Vite development server cache
/web/.angular/          # Example for Angular project specific files
/web/.next/             # Example for Next.js build output

# == Other common ignores (consider for 'docs' or 'assets' if applicable) ==
# Ignore common backup/system files
*~
*.bak